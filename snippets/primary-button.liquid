{% comment %}
  Renders a primary button with a 3D bounce flip animation.

  Accepts:
  - text: {String} Button text
  - link: {String} Button link (optional)
  - type: {String} Button type (optional, default: 'button')
  - class: {String} Optional extra classes
  - attributes: {String} Optional extra attributes
{% endcomment %}

{%- liquid
  assign tag = 'a'
  if link == blank
    assign tag = 'button'
  endif
-%}

<{{ tag }}
  {% if link != blank %}
    href="{{ link }}"
  {% endif %}
  class="primary-button {{ class }}"
  {% if type != blank %}
    type="{{ type }}"
  {% endif %}
  {{ attributes }}
>
  <div class="primary-button__wrapper">
    <div class="primary-button__face primary-button__face--front">
      {%- if icon != blank -%}
        <span class="primary-button__icon">{{ icon }}</span>
      {%- endif -%}
      {%- if text != blank -%}
        <span class="primary-button__text">{{ text }}</span>
      {%- endif -%}
      {%- if badge != blank and badge != '0' -%}
        <span class="primary-button__badge">{{ badge }}</span>
      {%- endif -%}
    </div>
    <div class="primary-button__face primary-button__face--back">
      {%- if icon != blank -%}
        <span class="primary-button__icon">{{ icon }}</span>
      {%- endif -%}
      {%- if text != blank -%}
        <span class="primary-button__text">{{ text }}</span>
      {%- endif -%}
      {%- if badge != blank and badge != '0' -%}
        <span class="primary-button__badge">{{ badge }}</span>
      {%- endif -%}
    </div>
  </div>
</{{ tag }}>
