{% comment %}
  Custom Cart Drawer - Matches custom cart page design
  Usage: {% render 'cart-drawer' %}
{% endcomment %}

{{ 'quantity-popover.css' | asset_url | stylesheet_tag }}
{{ 'component-card.css' | asset_url | stylesheet_tag }}

<script src="{{ 'cart.js' | asset_url }}" defer="defer"></script>
<script src="{{ 'cart-drawer.js' | asset_url }}" defer="defer"></script>

<style>
  /* Figma: Bag drawer - warm beige #F5F0E1, dark green #2E473E, pink CTA #E0A8B8 */
  :root {
    --cart-drawer-text: #2e473e;
    --cart-drawer-text-muted: #7a8b7e;
    --cart-drawer-pink: #e0a8b8;
    --cart-drawer-pink-shadow: #2e473e;
    --cart-drawer-card-bg: #e9e6d7;
    --cart-drawer-item-bg: #dcd9ca;
    --cart-drawer-border: #d1cdbc;
  }

  .drawer {
    visibility: hidden;
  }

  cart-drawer {
    position: fixed;
    top: 0;
    right: 0;
    width: 30vw;
    min-width: 360px;
    max-width: 440px;
    height: 100vh;
    z-index: 9999;
    transform: translateX(100%);
    transition: transform 0.6s cubic-bezier(0.4, 0, 0.2, 1);
    pointer-events: none;
  }

  cart-drawer.active {
    pointer-events: auto;
    transform: translateX(0);
  }

  cart-drawer.animate {
    visibility: visible;
  }

  .cart-drawer {
    width: 100%;
    height: 100%;
    display: flex;
    flex-direction: column;
    background-color: var(--color-header-button-background);
  }

  body.cart-drawer-open main,
  body.cart-drawer-open #MainContent,
  body.cart-drawer-open footer,
  body.cart-drawer-open [id^='shopify-section-']:not(cart-drawer),
  body.cart-drawer-open .floating-dock-header,
  body.cart-drawer-open .mobile-header,
  body.cart-drawer-open header {
    filter: blur(4px);
    transition: filter 0.3s ease;
    pointer-events: none;
  }

  body.cart-drawer-open cart-drawer,
  body.cart-drawer-open cart-drawer * {
    filter: none !important;
    pointer-events: auto !important;
  }

  .cart-drawer__overlay {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.05);
    backdrop-filter: blur(20px) saturate(180%);
    -webkit-backdrop-filter: blur(20px) saturate(180%);
    z-index: 0;
    opacity: 0;
    pointer-events: none;
    transition: opacity 0.3s ease;
  }

  body.cart-drawer-open .cart-drawer__overlay {
    opacity: 1;
    pointer-events: auto;
  }

  .drawer__inner {
    width: 100%;
    height: 100%;
    display: flex;
    flex-direction: column;
    overflow: hidden;
    background-color: var(--color-header-button-background);
    position: relative;
    z-index: 2;
    box-shadow: -4px 0 24px rgba(0, 0, 0, 0.05);
  }

  /* Scrollable area: header + product list (above subtotal) */
  .drawer__scrollable {
    flex: 1;
    min-height: 0;
    overflow-y: auto;
    display: flex;
    flex-direction: column;
  }

  /* Header: Bag + close X (scrolls with content) */
  .drawer__header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 20px 24px;
    background-color: var(--color-header-button-background);
    border-bottom: 1px solid var(--cart-drawer-border);
    flex-shrink: 0;
  }

  .drawer__heading {
    font-family: var(--font-heading-family);
    font-size: 64px;
    font-weight: 900;
    letter-spacing: -0.04em;
    margin: 0;
    color: var(--cart-drawer-text);
    line-height: 1;
  }

  .drawer__close {
    width: 40px;
    height: 40px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: none;
    border: none;
    cursor: pointer;
    color: var(--cart-drawer-text);
    padding: 0;
    border-radius: 8px;
    transition: background 0.2s ease;
  }

  .drawer__close:hover {
    background: var(--cart-drawer-border);
  }

  .drawer__close svg {
    width: 30px;
    height: 30px;
  }

  /* Product list (inside scrollable area) */
  .drawer__contents {
    flex: 1;
    min-height: 0;
    padding: 24px;
  }

  .drawer__cart-items-wrapper {
    margin-bottom: 24px;
  }

  .cart-drawer__items {
    display: flex;
    flex-direction: column;
    gap: 32px;
  }

  /* Product card: matching beige with subtle border */
  .cart-drawer__item {
    display: flex;
    flex-direction: column;
    gap: 16px;
    padding-bottom: 24px;
  }

  .cart-drawer__item-top {
    display: flex;
    gap: 16px;
    align-items: center;
  }

  .cart-drawer__item-image-wrap {
    position: relative;
    flex-shrink: 0;
  }

  .cart-drawer__item-image-container {
    width: 120px;
    border-radius: 12px;
    overflow: hidden;
    background: var(--cart-drawer-item-bg);
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 8px;
  }

  .cart-drawer__item-image {
    max-width: 100%;
    max-height: 100%;
    object-fit: contain;
  }

  /* 16% off badge - pink, starburst */
  .cart-drawer__item-badge {
    position: absolute;
    top: -10px;
    left: -10px;
    width: 50px;
    height: 50px;
    background: var(--cart-drawer-pink);
    color: var(--cart-drawer-text);
    font-size: 10px;
    font-weight: 900;
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    padding: 4px;
    line-height: 1;
    z-index: 2;
    clip-path: polygon(
      50% 0%,
      61% 10%,
      75% 5%,
      79% 19%,
      93% 18%,
      90% 32%,
      100% 41%,
      93% 54%,
      98% 68%,
      85% 74%,
      81% 88%,
      68% 87%,
      58% 97%,
      45% 90%,
      32% 96%,
      23% 84%,
      9% 84%,
      12% 70%,
      2% 61%,
      10% 48%,
      3% 34%,
      16% 29%,
      17% 15%,
      31% 18%,
      39% 7%
    );
  }

  .cart-drawer__item-details {
    flex: 1;
    min-width: 0;
    display: flex;
    flex-direction: column;
    gap: 6px;
  }

  .cart-drawer__item-details-top {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 8px;
  }

  .cart-drawer__item-details-top > div {
    max-width: 150px;
  }

  /* X remove button - right of product name */
  cart-remove-button {
    flex-shrink: 0;
  }

  .cart-drawer__item-remove {
    display: inline-flex;
    align-items: center;
    justify-content: center;

    padding: 0;
    background: transparent;
    color: var(--cart-drawer-text);
    cursor: pointer;
    text-decoration: none;
    transition: background 0.2s ease, color 0.2s ease;
  }

  .cart-drawer__item-remove:hover {
    background: var(--cart-drawer-text);
    background-color: var(--color-header-button-background);
  }

  .cart-drawer__item-remove svg {
    width: 30px;
    height: 30px;
    stroke-width: 2;
  }

  .cart-drawer__item-name {
    font-family: var(--font-heading-family);
    font-size: 20px;
    font-weight: 800;
    letter-spacing: 0.02em;
    color: var(--cart-drawer-text);
    text-decoration: none;
    line-height: 1.1;
    text-transform: uppercase;
    margin-bottom: 2px;
  }

  .cart-drawer__item-name:hover {
    opacity: 0.8;
  }

  .cart-drawer__item-variant {
    font-size: 13px;
    color: var(--cart-drawer-text-muted);
    font-weight: 700;
    margin-bottom: 8px;
  }

  .cart-drawer__item-price-row {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: 8px;
    margin-top: 4px;
  }

  .cart-drawer__item-price-label {
    font-size: 18px;
    font-weight: 300;
    color: var(--cart-drawer-text);
    min-width: 60px;
  }

  .cart-drawer__item-price {
    display: flex;
    align-items: baseline;
    gap: 6px;
    font-weight: 400;
    color: var(--cart-drawer-text);
    font-size: 18px;
  }

  .cart-drawer__item-price s {
    color: var(--cart-drawer-text-muted);
    font-weight: 600;
    font-size: 14px;
    text-decoration: line-through;
  }

  .cart-drawer__item-qty-row {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 8px;
    margin-top: 4px;
  }

  .cart-drawer__item-qty-label {
    font-size: 18px;
    font-weight: 300;
    color: var(--cart-drawer-text);
    min-width: 60px;
  }

  .cart-drawer__item-qty-value {
    font-size: 18px;
    font-weight: 400;
    color: var(--cart-drawer-text);
  }

  /* Quantity selector: [-] [1] [+] + Edit */
  /* Separator line before quantity selector */
  .cart-drawer__quantity-row {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 12px;
    margin-top: 12px;
    padding-top: 12px;
    border-top: 1px solid var(--cart-drawer-border);
  }

  .cart-drawer__quantity-selector {
    display: flex;
    align-items: center;
    gap: 6px;
  }

  .cart-drawer__quantity-selector .quantity__button {
    width: 38px;
    height: 38px;
    background: var(--color-background);
    border: 1px solid var(--color-headings);
    border-radius: 10px;
    cursor: pointer;
    color: var(--color-headings);
    font-size: 24px;
    font-weight: 400;
    padding: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: 0 2px 0 #000;
    transition: transform 0.2s ease, box-shadow 0.2s ease;
  }

  .cart-drawer__quantity-selector .quantity__button:hover {
    transform: translateY(-4px);
    box-shadow: 0 6px 0 #000;
  }

  .cart-drawer__quantity-selector .quantity__button:active {
    transform: translateY(1px);
    box-shadow: 0 1px 0 #000;
  }

  .cart-drawer__quantity-input {
    width: 32px !important;
    height: 32px !important;
    min-width: 32px;
    min-height: 32px;
    background: var(--color-headings) !important;
    color: var(--color-background) !important;
    border: 1px solid #000 !important;
    border-radius: 10px;
    text-align: center;
    font-weight: 700 !important;
    font-size: 16px !important;
    padding: 0 !important;
    margin: 0 !important;
    box-shadow: 0 2px 0 #000;
    transition: transform 0.2s ease, box-shadow 0.2s ease;
  }

  .cart-drawer__quantity-input:hover {
    transform: translateY(-4px);
    box-shadow: 0 6px 0 #000;
  }

  .cart-drawer__quantity-input:active,
  .cart-drawer__quantity-input:focus {
    transform: translateY(1px);
    box-shadow: 0 1px 0 #000;
  }

  .cart-drawer__quantity-input::-webkit-outer-spin-button,
  .cart-drawer__quantity-input::-webkit-inner-spin-button {
    -webkit-appearance: none;
    margin: 0;
  }

  .cart-drawer__edit-btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 8px 32px;
    background-color: var(--color-header-button-background);
    color: var(--cart-drawer-text);
    border: 1.5px solid var(--cart-drawer-text);
    border-radius: 10px;
    font-size: 16px;
    font-weight: 800;
    text-decoration: none;
    font-family: var(--font-heading-family);
    transition: background 0.2s ease, color 0.2s ease;
    height: 38px;
    box-shadow: 0 1px 0 var(--cart-drawer-text);
  }

  .cart-drawer__edit-btn:hover {
    background: var(--cart-drawer-text);
    background-color: var(--color-header-button-background);
  }

  /* Fixed footer: subtotal + checkout + GO TO BAG (does not scroll) */
  .cart-drawer__sidebar {
    flex-shrink: 0;
    padding: 20px 24px 24px;
    border-top: 1px solid var(--cart-drawer-border);
    background-color: var(--color-header-button-background);
  }

  .cart-drawer__total-row {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 16px;
    margin-bottom: 24px;
  }

  .cart-drawer__total-left {
    display: flex;
    flex-direction: column;
    min-width: 200px;
    gap: 4px;
  }

  .cart-drawer__total-label {
    font-size: 20px;
    font-weight: 900;
    color: var(--cart-drawer-text);
    text-transform: uppercase;
    letter-spacing: 0.02em;
    line-height: 1;
  }

  .cart-drawer__shipping-note {
    font-size: 16px;
    font-weight: 500;
    color: var(--cart-drawer-text);
    line-height: 1.1;
    max-width: 220px;
    margin: 0;
  }

  .cart-drawer__total-value {
    font-size: 28px;
    font-weight: 900;
    color: var(--cart-drawer-text);
    letter-spacing: -0.04em;
    line-height: 0.8;
  }

  /* Proceed To Checkout - pink with 3D shadow */
  .cart-drawer__checkout-btn {
    width: 100%;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 20px 24px;
    background-color: var(--cart-drawer-pink);
    color: var(--cart-drawer-text);
    border: 2px solid var(--cart-drawer-text);
    border-radius: 16px;
    font-size: 22px;
    font-weight: 900;
    cursor: pointer;
    text-transform: capitalize;
    letter-spacing: -0.02em;
    font-family: var(--font-heading-family);
    line-height: 1;
    text-decoration: none;
    text-align: center;
    box-shadow: 0 8px 0 var(--cart-drawer-text);
    transition: transform 0.1s ease, box-shadow 0.1s ease;
    margin-bottom: 24px;
  }

  .cart-drawer__checkout-btn:hover:not(:disabled) {
    transform: translateY(2px);
    box-shadow: 0 6px 0 var(--cart-drawer-text);
  }

  .cart-drawer__checkout-btn:active:not(:disabled) {
    transform: translateY(6px);
    box-shadow: 0 2px 0 var(--cart-drawer-text);
  }

  .cart-drawer__checkout-btn:disabled {
    opacity: 0.6;
    cursor: not-allowed;
  }

  .cart-drawer__go-to-bag {
    display: block;
    text-align: center;
    font-size: 18px;
    font-weight: 800;
    color: var(--cart-drawer-text);
    text-transform: uppercase;
    letter-spacing: 0.08em;
    text-decoration: none;
  }

  .cart-drawer__go-to-bag:hover {
    text-decoration: underline;
  }

  /* Empty state */
  .drawer__inner-empty {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    height: 100%;
    text-align: center;
    padding: 40px 24px;
  }

  .cart__empty-text {
    font-family: var(--font-heading-family);
    font-size: 22px;
    font-weight: 700;
    margin-bottom: 20px;
    color: var(--cart-drawer-text);
  }

  @media screen and (max-width: 990px) {
    cart-drawer {
      width: 100vw;
      max-width: none;
      min-width: 0;
    }

    .drawer__inner {
      border-radius: 0;
    }
  }

  /* Edit panel overlay (replaces list when editing an item) */
  .cart-drawer__edit-panel {
    display: none;
    flex-direction: column;
    flex: 1;
    min-height: 0;
    overflow: hidden;
    background-color: var(--color-header-button-background);
  }

  cart-drawer.editing .cart-drawer__edit-panel {
    display: flex;
  }

  cart-drawer.editing .drawer__scrollable {
    display: none;
  }

  cart-drawer.editing .cart-drawer__sidebar {
    display: none;
  }

  .cart-drawer__edit-panel-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 20px 24px;
    border-bottom: 1px solid var(--cart-drawer-border);
    flex-shrink: 0;
  }

  .cart-drawer__edit-panel-heading {
    font-family: var(--font-heading-family);
    font-size: 64px;
    font-weight: 900;
    letter-spacing: -0.04em;
    margin: 0;
    color: var(--cart-drawer-text);
    line-height: 1;
  }

  .cart-drawer__edit-panel-close {
    width: 40px;
    height: 40px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: none;
    border: none;
    cursor: pointer;
    color: var(--cart-drawer-text);
    padding: 0;
    border-radius: 8px;
    transition: background 0.2s ease;
  }

  .cart-drawer__edit-panel-close:hover {
    background: var(--cart-drawer-border);
  }

  .cart-drawer__edit-panel-close svg {
    width: 30px;
    height: 30px;
  }

  .cart-drawer__edit-panel-body {
    flex: 1;
    min-height: 0;
    overflow-y: auto;
    padding: 24px;
    display: flex;
    flex-direction: column;
    gap: 20px;
  }

  .cart-drawer__edit-item-top {
    display: flex;
    gap: 16px;
    align-items: flex-start;
  }

  .cart-drawer__edit-item-image-container {
    width: 120px;
    flex-shrink: 0;
    border-radius: 12px;
    overflow: hidden;
    background: var(--cart-drawer-item-bg);
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 8px;
  }

  .cart-drawer__edit-item-image {
    max-width: 100%;
    max-height: 100%;
    object-fit: contain;
  }

  .cart-drawer__edit-item-details {
    flex: 1;
    min-width: 0;
  }

  .cart-drawer__edit-item-name {
    font-family: var(--font-heading-family);
    font-size: 20px;
    font-weight: 800;
    letter-spacing: 0.02em;
    color: var(--cart-drawer-text);
    line-height: 1.1;
    text-transform: uppercase;
    margin: 0 0 8px 0;
  }

  .cart-drawer__edit-item-price {
    font-size: 18px;
    font-weight: 400;
    color: var(--cart-drawer-text);
    margin-bottom: 12px;
  }

  .cart-drawer__edit-item-price s {
    color: var(--cart-drawer-text-muted);
    font-size: 14px;
    text-decoration: line-through;
  }

  .cart-drawer__edit-quantity-row {
    display: flex;
    align-items: center;
    gap: 6px;
    margin-bottom: 8px;
  }

  .cart-drawer__edit-quantity-row .quantity__button {
    width: 38px;
    height: 38px;
    background-color: var(--color-background);
    border: 1px solid var(--color-headings);
    border-radius: 10px;
    cursor: pointer;
    color: var(--color-headings);
    font-size: 24px;
    font-weight: 400;
    padding: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: 0 2px 0 #000;
    transition: transform 0.2s ease, box-shadow 0.2s ease;
  }

  .cart-drawer__edit-quantity-row .quantity__button:hover {
    transform: translateY(-4px);
    box-shadow: 0 6px 0 #000;
  }

  .cart-drawer__edit-quantity-row .quantity__button:active {
    transform: translateY(1px);
    box-shadow: 0 1px 0 #000;
  }

  .cart-drawer__edit-quantity-row .quantity__input {
    width: 32px !important;
    height: 32px !important;
    min-width: 32px;
    min-height: 32px;
    background: var(--color-headings) !important;
    color: var(--color-background) !important;
    border: 1px solid #000 !important;
    border-radius: 10px;
    text-align: center;
    font-weight: 700 !important;
    font-size: 16px !important;
    padding: 0 !important;
    margin: 0 !important;
    box-shadow: 0 2px 0 #000;
    transition: transform 0.2s ease, box-shadow 0.2s ease;
  }

  .cart-drawer__edit-quantity-row .quantity__input:hover {
    transform: translateY(-4px);
    box-shadow: 0 6px 0 #000;
  }

  .cart-drawer__edit-quantity-row .quantity__input:active,
  .cart-drawer__edit-quantity-row .quantity__input:focus {
    transform: translateY(1px);
    box-shadow: 0 1px 0 #000;
  }

  .cart-drawer__edit-stock-wrap {
    margin-top: 16px;
    padding-top: 16px;
    border-top: 1px solid var(--cart-drawer-border);
    text-align: center;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-direction: column;
  }

  .cart-drawer__edit-stock {
    font-size: 14px;
    font-weight: 600;
    color: var(--cart-drawer-text);
    margin-bottom: 12px;
  }

  .cart-drawer__edit-variants {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
    margin-bottom: 24px;
  }

  .cart-drawer__edit-variant-btn {
    padding: 10px 20px;
    background-color: var(--color-header-button-background);
    color: var(--cart-drawer-text);
    border: 1.5px solid var(--cart-drawer-text);
    border-radius: 10px;
    font-size: 14px;
    font-weight: 700;
    cursor: pointer;
    font-family: var(--font-heading-family);
    transition: background 0.2s ease, color 0.2s ease;
  }

  .cart-drawer__edit-variant-btn:hover {
    background: var(--cart-drawer-border);
  }

  .cart-drawer__edit-variant-btn.is-selected {
    background: var(--cart-drawer-pink);
    color: var(--cart-drawer-text);
  }

  .cart-drawer__edit-update-wrap {
    margin-top: auto;
    padding-top: 24px;
    border-top: 1px solid var(--cart-drawer-border);
    width: 100%;
  }

  .cart-drawer__edit-update-btn {
    width: 100%;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 20px 24px;
    background-color: var(--cart-drawer-pink);
    color: var(--cart-drawer-text);
    border: 2px solid var(--cart-drawer-text);
    border-radius: 16px;
    font-size: 22px;
    font-weight: 900;
    cursor: pointer;
    text-transform: capitalize;
    letter-spacing: -0.02em;
    font-family: var(--font-heading-family);
    line-height: 1;
    box-shadow: 0 8px 0 var(--cart-drawer-text);
    transition: transform 0.1s ease, box-shadow 0.1s ease;
  }

  .cart-drawer__edit-update-btn:hover:not(:disabled) {
    transform: translateY(2px);
    box-shadow: 0 6px 0 var(--cart-drawer-text);
  }

  .cart-drawer__edit-update-btn:disabled {
    opacity: 0.6;
    cursor: not-allowed;
  }

  @media screen and (max-width: 640px) {
    .drawer__header {
      padding: 16px 20px;
    }

    .drawer__heading {
      font-size: 24px;
    }

    .drawer__contents {
      padding: 12px 20px;
    }

    .cart-drawer__item-image-container {
      width: 72px;
      height: 72px;
    }

    .cart-drawer__item-name {
      font-size: 13px;
    }

    .cart-drawer__sidebar {
      padding: 16px 20px 20px;
    }

    .cart-drawer__total-value {
      font-size: 20px;
    }

    .cart-drawer__edit-panel-heading {
      font-size: 32px;
    }

    .cart-drawer__edit-item-image-container {
      width: 72px;
    }

    .cart-drawer__edit-item-name {
      font-size: 16px;
    }
  }
</style>

<cart-drawer class="drawer{% if cart == empty %} is-empty{% endif %}">
  <div id="CartDrawer" class="cart-drawer">
    <div class="cart-drawer__overlay"></div>
    <div
      class="drawer__inner"
      role="dialog"
      aria-modal="true"
      aria-label="{{ 'sections.cart.title' | t }}"
      tabindex="-1"
    >
      {%- if cart == empty -%}
        <div class="drawer__header">
          <h2 class="drawer__heading">Bag</h2>
          <button
            type="button"
            class="drawer__close"
            aria-label="{{ 'accessibility.close' | t }}"
          >
            <svg
              xmlns="http://www.w3.org/2000/svg"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
            >
              <path d="M18 6L6 18M6 6l12 12"/>
            </svg>
          </button>
        </div>
        <div class="drawer__inner-empty">
          <h2 class="cart__empty-text">{{ 'sections.cart.empty' | t }}</h2>
          <a href="{{ routes.all_products_collection_url }}" class="cart-drawer__checkout-btn">
            {{ 'general.continue_shopping' | t }}
          </a>
        </div>
      {%- else -%}
        <div class="cart-drawer__edit-panel" id="CartDrawer-EditPanel" aria-hidden="true">
          <div class="cart-drawer__edit-panel-header">
            <h2 class="cart-drawer__edit-panel-heading">Edit</h2>
            <button
              type="button"
              class="cart-drawer__edit-panel-close"
              aria-label="{{ 'accessibility.close' | t }}"
              data-edit-panel-close
            >
              <svg
                xmlns="http://www.w3.org/2000/svg"
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                stroke-width="2"
              >
                <path d="M18 6L6 18M6 6l12 12"/>
              </svg>
            </button>
          </div>
          <div class="cart-drawer__edit-panel-body">
            <div class="cart-drawer__edit-item-top">
              <div class="cart-drawer__edit-item-image-container">
                <img class="cart-drawer__edit-item-image" src="" alt="" width="100" height="100" data-edit-image>
              </div>
              <div class="cart-drawer__edit-item-details">
                <h3 class="cart-drawer__edit-item-name" data-edit-name></h3>
                <div class="cart-drawer__edit-item-price" data-edit-price></div>
                <div class="cart-drawer__edit-quantity-row">
                  <quantity-input class="cart-drawer__quantity-selector" data-edit-quantity-wrapper>
                    <button
                      class="quantity__button"
                      type="button"
                      name="minus"
                      aria-label="{{ 'products.product.quantity.decrease' | t | default: 'Decrease' }}"
                      data-edit-qty-minus
                    >
                      −
                    </button>
                    <input
                      class="quantity__input cart-drawer__quantity-input"
                      type="number"
                      name="edit_quantity"
                      value="1"
                      min="1"
                      step="1"
                      data-min="1"
                      data-edit-qty-input
                      aria-label="Quantity"
                    >
                    <button
                      class="quantity__button"
                      type="button"
                      name="plus"
                      aria-label="{{ 'products.product.quantity.increase' | t | default: 'Increase' }}"
                      data-edit-qty-plus
                    >
                      +
                    </button>
                  </quantity-input>
                </div>
              </div>
            </div>
            <div class="cart-drawer__edit-stock-wrap">
              <div class="cart-drawer__edit-stock" data-edit-stock>In Stock</div>
              <div class="cart-drawer__edit-variants" data-edit-variants></div>
            </div>
            <div class="cart-drawer__edit-update-wrap">
              <button type="button" class="cart-drawer__edit-update-btn" data-edit-update-btn>Update Cart</button>
            </div>
          </div>
        </div>

        <div class="drawer__scrollable">
          <div class="drawer__header">
            <h2 class="drawer__heading">Bag</h2>
            <button
              type="button"
              class="drawer__close"
              aria-label="{{ 'accessibility.close' | t }}"
            >
              <svg
                xmlns="http://www.w3.org/2000/svg"
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                stroke-width="2"
                stroke-linecap="round"
                stroke-linejoin="round"
              >
                <path d="M18 6L6 18M6 6l12 12"/>
              </svg>
            </button>
          </div>

          <cart-drawer-items>
            <form
              action="{{ routes.cart_url }}"
              id="CartDrawer-Form"
              class="cart-drawer__form"
              method="post"
            >
              <div id="CartDrawer-CartItems" class="drawer__contents js-contents">
                <div class="drawer__cart-items-wrapper">
                  <div class="cart-drawer__items">
                    {%- for item in cart.items -%}
                      {%- assign has_compare = false -%}
                      {%- assign discount_pct = 0 -%}
                      {%- if item.variant.compare_at_price != blank
                        and item.variant.compare_at_price > item.final_price
                      -%}
                        {%- assign has_compare = true -%}
                        {%- assign savings = item.variant.compare_at_price | minus: item.final_price -%}
                        {%- assign discount_pct = savings | times: 100 | divided_by: item.variant.compare_at_price -%}
                      {%- endif -%}
                      <div class="cart-drawer__item" id="CartDrawer-Item-{{ item.index | plus: 1 }}">
                        <script
                          type="application/json"
                          id="CartDrawer-EditData-{{ item.index | plus: 1 }}"
                          data-edit-data
                        >
                          {
                            "lineIndex": {{ item.index | plus: 1 }},
                            "variantId": {{ item.variant_id }},
                            "quantity": {{ item.quantity }},
                            "image": {{ item.image | image_url: width: 200 | json }},
                            "title": {{ item.product.title | json }},
                            "price": {{ item.final_price | json }},
                            "priceFormatted": {{ item.final_price | money_without_trailing_zeros | json }},
                            "compareAtPrice": {{ item.variant.compare_at_price | default: 0 | json }},
                            "compareAtPriceFormatted": {% if item.variant.compare_at_price != blank %}{{ item.variant.compare_at_price | money_without_trailing_zeros | json }}{% else %}null{% endif %},
                            "available": {{ item.variant.available | json }},
                            "optionsWithValues": {{ item.product.options_with_values | json }},
                            "variants": [
                              {%- for v in item.product.variants -%}
                                {
                                  "id": {{ v.id }},
                                  "title": {{ v.title | json }},
                                  "options": [{% if v.option1 != blank %}{{ v.option1 | json }}{% endif %}{% if v.option2 != blank %}{% if v.option1 != blank %}, {% endif %}{{ v.option2 | json }}{% endif %}{% if v.option3 != blank %}{% if v.option1 != blank or v.option2 != blank %}, {% endif %}{{ v.option3 | json }}{% endif %}],
                                  "price": {{ v.price }},
                                  "priceFormatted": {{ v.price | money_without_trailing_zeros | json }},
                                  "compareAtPrice": {{ v.compare_at_price | default: 0 }},
                                  "compareAtPriceFormatted": {% if v.compare_at_price != blank %}{{ v.compare_at_price | money_without_trailing_zeros | json }}{% else %}null{% endif %},
                                  "available": {{ v.available | json }}
                                }{% unless forloop.last %},{% endunless %}
                              {%- endfor -%}
                            ]
                          }
                        </script>
                        <div class="cart-drawer__item-top">
                          <div class="cart-drawer__item-image-wrap">
                            {%- if has_compare and discount_pct > 0 -%}
                              <span class="cart-drawer__item-badge">
                                {{- discount_pct }}%<br>
                                off</span
                              >
                            {%- endif -%}
                            <div class="cart-drawer__item-image-container">
                              {%- if item.image -%}
                                <img
                                  src="{{ item.image | image_url: width: 200 }}"
                                  alt="{{ item.image.alt | escape }}"
                                  class="cart-drawer__item-image"
                                  loading="lazy"
                                  width="100"
                                  height="100"
                                >
                              {%- endif -%}
                            </div>
                          </div>
                          <div class="cart-drawer__item-details">
                            <div class="cart-drawer__item-details-top">
                              <div style="flex: 1;">
                                <a href="{{ item.url }}" class="cart-drawer__item-name">
                                  {{- item.product.title | upcase -}}
                                </a>
                                {%- unless item.product.has_only_default_variant -%}
                                  <div class="cart-drawer__item-variant">{{ item.variant.title }}</div>
                                {%- endunless -%}
                              </div>
                              <cart-remove-button
                                id="Remove-{{ item.index | plus: 1 }}"
                                data-index="{{ item.index | plus: 1 }}"
                              >
                                <a
                                  href="{{ item.url_to_remove }}"
                                  class="cart-drawer__item-remove"
                                  aria-label="{{ 'sections.cart.remove_title' | t: title: item.title | escape }}"
                                >
                                  <svg
                                    xmlns="http://www.w3.org/2000/svg"
                                    viewBox="0 0 24 24"
                                    fill="none"
                                    stroke="currentColor"
                                    stroke-width="3"
                                  >
                                    <path d="M18 6L6 18M6 6l12 12"/>
                                  </svg>
                                </a>
                              </cart-remove-button>
                            </div>
                            <div class="cart-drawer__item-price-row">
                              <span class="cart-drawer__item-price-label">Price:</span>
                              <span class="cart-drawer__item-price">
                                {%- if has_compare -%}
                                  <s>{{ item.variant.compare_at_price | money_without_trailing_zeros }}</s>
                                {%- endif -%}
                                {{ item.final_price | money_without_trailing_zeros }}
                              </span>
                            </div>
                            <div class="cart-drawer__item-qty-row">
                              <span class="cart-drawer__item-qty-label">Quantity:</span>
                              <span class="cart-drawer__item-qty-value">{{ item.quantity }}</span>
                            </div>
                          </div>
                        </div>
                        <div class="cart-drawer__quantity-row">
                          <quantity-input class="cart-drawer__quantity-selector">
                            <button
                              class="quantity__button"
                              name="minus"
                              type="button"
                              aria-label="{{ 'products.product.quantity.decrease' | t | default: 'Decrease' }}"
                            >
                              <span>−</span>
                            </button>
                            <input
                              class="quantity__input cart-drawer__quantity-input"
                              type="number"
                              name="updates[]"
                              value="{{ item.quantity }}"
                              data-index="{{ item.index | plus: 1 }}"
                              min="0"
                              id="Drawer-quantity-{{ item.index | plus: 1 }}"
                              aria-label="{{ 'products.product.quantity.input_label' | t: product: item.product.title | default: 'Quantity' }}"
                            >
                            <button
                              class="quantity__button"
                              name="plus"
                              type="button"
                              aria-label="{{ 'products.product.quantity.increase' | t | default: 'Increase' }}"
                            >
                              <span>+</span>
                            </button>
                          </quantity-input>
                          <button
                            type="button"
                            class="cart-drawer__edit-btn"
                            data-edit-trigger
                            data-line-index="{{ item.index | plus: 1 }}"
                            aria-label="Edit {{ item.product.title | escape }}"
                          >
                            Edit
                          </button>
                        </div>
                      </div>
                    {%- endfor -%}
                  </div>
                </div>
              </div>
            </form>
          </cart-drawer-items>
        </div>

        <div class="cart-drawer__sidebar">
          <div class="cart-drawer__total-row">
            <div class="cart-drawer__total-left">
              <span class="cart-drawer__total-label">SUBTOTAL</span>
              <p class="cart-drawer__shipping-note">
                Taxes And Shipping<br>
                Calculated At Checkout
              </p>
            </div>
            <span class="cart-drawer__total-value" id="cart-drawer-total">
              {{- cart.total_price | money_without_trailing_zeros -}}
            </span>
          </div>
          <button
            type="submit"
            form="CartDrawer-Form"
            name="checkout"
            id="CartDrawer-Checkout"
            class="cart-drawer__checkout-btn"
          >
            Proceed To Checkout
          </button>
          <a href="{{ routes.cart_url }}" class="cart-drawer__go-to-bag">GO TO BAG</a>
        </div>
      {%- endif -%}
    </div>
  </div>
</cart-drawer>
