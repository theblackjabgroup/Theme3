{%- liquid
  assign price_range_max = filter.range_max
  comment
    Slider initial values: use 0 and range_max; JS syncs from text inputs (money_without_currency) on init.
    Shopify Liquid Money does not expose .fractional reliably, so we rely on the existing text input values.
  endcomment
  assign price_slider_min_val = 0
  assign price_slider_max_val = price_range_max
-%}
<div class="price-facet-slider" data-price-range-max="{{ price_range_max }}" data-price-min-input-id="{{ id_prefix }}{{ filter.label | escape }}-GTE" data-price-max-input-id="{{ id_prefix }}{{ filter.label | escape }}-LTE">
  <div class="price-facet-slider__track">
    <div class="price-facet-slider__fill"></div>
    <input
      type="range"
      class="price-facet-slider__input price-facet-slider__input--min"
      min="0"
      max="{{ price_range_max }}"
      value="{{ price_slider_min_val }}"
      aria-label="{{ 'products.facets.from' | t | default: 'Min' }}"
    >
    <input
      type="range"
      class="price-facet-slider__input price-facet-slider__input--max"
      min="0"
      max="{{ price_range_max }}"
      value="{{ price_slider_max_val }}"
      aria-label="{{ 'products.facets.to' | t | default: 'Max' }}"
    >
  </div>
</div>
<div class="price-facet-inputs-container">
  <div class="field">
    <span class="field-currency">{{ cart.currency.symbol }}</span>
    <input
      class="field__input price-facet-input-min"
      name="{{ filter.min_value.param_name }}"
      id="{{ id_prefix }}{{ filter.label | escape }}-GTE"
      {%- if filter.min_value.value -%}
        value="{{ filter.min_value.value | money_without_currency }}"
      {%- endif -%}
      type="text"
      inputmode="decimal"
      placeholder="0"
      data-pattern="\d| |,|\."
      data-min="0"
      data-max="{{ filter.range_max | money_without_currency }}"
    >
  </div>

  <span class="price-range-separator">-</span>

  <div class="field">
    <span class="field-currency">{{ cart.currency.symbol }}</span>
    <input
      class="field__input price-facet-input-max"
      name="{{ filter.max_value.param_name }}"
      id="{{ id_prefix }}{{ filter.label | escape }}-LTE"
      {%- if filter.max_value.value -%}
        value="{{ filter.max_value.value | money_without_currency }}"
      {%- endif -%}
      type="text"
      inputmode="decimal"
      placeholder="{{ filter.range_max | money_without_currency }}"
      data-pattern="\d| |,|\."
      data-min="0"
      data-max="{{ filter.range_max | money_without_currency }}"
    >
  </div>
</div>
